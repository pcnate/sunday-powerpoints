import { NgModule } from '@angular/core';
import { AppComponent } from './app/app.component';

@NgModule({
  declarations: [
    AppComponent,
    // SongLibraryComponent,
    // UpcomingSongsComponent,
    // AdminComponent,
    // OutputFoldersSidebarComponent,
    // NotesModalComponent,
    // SongSelectModalComponent,
    // SongBadgeComponent,
    // ThemeToggle,
  ],
  imports: [
    // BrowserModule,
    // BrowserAnimationsModule,
    // MaterialModule,
  ],
  // providers: [SocketService],
  bootstrap: [AppComponent]
})
export class AppModule { }
